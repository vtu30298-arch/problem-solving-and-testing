class Solution {
    public boolean halvesAreAlike(String s) {
        int half = s.length() / 2;
        int count = 0;

        for (int i = 0; i < half; i++) {
            if (isVowel(s.charAt(i))) {
                count++;
            }
        }
